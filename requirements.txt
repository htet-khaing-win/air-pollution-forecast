# Air Pollution Forecast — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Data ingestion ─────────────────────────────────────────
httpx>=0.27.0              # Modern async-capable HTTP client (faster than requests)
pandas>=2.2.0              # DataFrames and time-series handling
python-dotenv>=1.0.0       # Load .env file into environment variables

# ── Model training (Phase 3) ──────────────────────────────
xgboost>=2.1.0             # Gradient boosting model
scikit-learn>=1.5.0        # Train/test split, metrics, preprocessing

# ── Experiment tracking (Phase 3) ─────────────────────────
mlflow>=2.18.0             # Experiment logging, model registry

# ── API serving (Phase 5) ─────────────────────────────────
fastapi>=0.115.0           # Prediction REST API
uvicorn>=0.32.0            # ASGI server for FastAPI

# ── Dashboard (Phase 7) ──────────────────────────────────
streamlit>=1.40.0          # Interactive dashboard

# ── Monitoring (Phase 8) ─────────────────────────────────
prometheus-client>=0.21.0  # Expose metrics for Prometheus

# ── Testing ──────────────────────────────────────────────
pytest>=8.3.0              # Test runner
pytest-httpx>=0.34.0       # Mock httpx requests in tests

lightgbm
prophet
pyyaml
chronos-forecasting
optuna
apache-airflow
fastapi
uvicorn
psycopg2-binary